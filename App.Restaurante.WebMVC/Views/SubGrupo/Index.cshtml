@model IEnumerable<App.Restaurante.Models.SubGrupo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdSubGrupo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdGrupo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdSubGrupo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdGrupo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdSubGrupo }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdSubGrupo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdSubGrupo })
        </td>
    </tr>
}

</table>
